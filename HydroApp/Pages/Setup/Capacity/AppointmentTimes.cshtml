@page
@model HydroApp.Pages.Setup.Capacity.AppointmentTimesModel
@{
    ViewData["Title"] = "AppointmentTimes";
    Layout = "_Layout";
}

<h2>AppointmentTimes</h2>

<div class="card">
    <div class="card-body">
        <h5 class="card-title">AppointmentTimes Configuration</h5>
        <p class="card-text">Configure appointmenttimes settings for capacity management.</p>
        <div class="alert alert-info">
            <i class="bi bi-info-circle me-2"></i>
            AppointmentTimes configuration will be implemented here.
        </div>
    </div>
</div>
