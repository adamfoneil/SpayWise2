@page
@model HydroApp.Pages.Calendar.IndexModel
@{
    ViewData["Title"] = "Calendar";
}

<h2>Calendar</h2>

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Appointment Calendar</h5>
                <p class="card-text">View and manage appointment schedules and calendar events.</p>
                <div class="alert alert-info">
                    <i class="bi bi-info-circle me-2"></i>
                    Calendar functionality will be implemented here.
                </div>
            </div>
        </div>
    </div>
</div>